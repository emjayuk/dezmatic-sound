import EventsPageClient from '@/components/EventsPageClient'

export const metadata = {
  title: 'Events | Dezmatic Sound — Bristol Sound System',
  description: 'Dezmatic Sound covers weddings, club nights, carnivals, corporate events, private hire, ceremonies and more across Bristol and the UK.',
}

export default function EventsPage() {
  return <EventsPageClient />
}
