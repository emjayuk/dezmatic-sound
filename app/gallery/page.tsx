import GalleryPageClient from '@/components/GalleryPageClient'

export const metadata = {
  title: 'Gallery | Dezmatic Sound — Bristol Sound System',
  description: 'Photos and videos from Dezmatic Sound events across Bristol — weddings, carnivals, club nights and more.',
}

export default function GalleryPage() {
  return <GalleryPageClient />
}
